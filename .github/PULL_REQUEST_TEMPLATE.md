## Summary

<!-- Why this change exists. Link the issue if there is one. -->

## Test plan

- [ ] `python -m pytest tests/`
- [ ] `pre-commit run --all-files` (or CI green)
- [ ] Docs / `.env.example` updated if the API or configuration changed
