# Override: build the image from this checkout instead of pulling it from GHCR.
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  audio-intel:
    image: audio-intel:local
    build:
      context: .
      dockerfile: Dockerfile
